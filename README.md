# azure-golang-sdk-sandbox
My own personal sandbox to try out the Azure Golang SDK

#### You need to modify the `./pkg/config/config.go` file first to make this code works.

More informations will be added later